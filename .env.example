# Optional runtime override for development or a custom OAuth desktop client.
# Official release packages contain a build-time injected public Client ID.
AGY_GOOGLE_OAUTH_CLIENT_ID=
# Only for a custom client that explicitly requires it. Never place a real secret in a release build.
AGY_GOOGLE_OAUTH_CLIENT_SECRET=
